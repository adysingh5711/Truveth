export interface Certificate {
    id: string
    studentName: string
    rollNumber: string
    institution: string
    course: string
    issueDate: number
    ipfsHash: string
    isValid: boolean
}